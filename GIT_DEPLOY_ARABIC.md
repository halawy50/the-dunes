# 🚀 رفع التحديثات مباشرة على Git - النشر التلقائي

## ✅ الوضع الحالي

- ✅ GitHub Actions workflow جاهز (`.github/workflows/deploy-vercel.yml`)
- ✅ يبدأ النشر تلقائياً عند push على `main` أو `master` أو `development`
- ⚠️ يحتاج إضافة Secrets في GitHub

## 📋 الخطوات المطلوبة

### الخطوة 1: الحصول على Vercel Credentials

#### 1.1: إنشاء Vercel Token

1. اذهب إلى: [vercel.com/account/tokens](https://vercel.com/account/tokens)
2. اضغط **"Create Token"**
3. اكتب اسم: `GitHub Actions`
4. اضغط **"Create"**
5. **انسخ الـ Token** واحفظه (لن يظهر مرة أخرى!)

#### 1.2: الحصول على Project ID و Org ID

1. اذهب إلى [vercel.com/dashboard](https://vercel.com/dashboard)
2. افتح مشروع **"the-dunes"**
3. اضغط **Settings** (في القائمة الجانبية)
4. اضغط **General**
5. ابحث عن:
   - **Project ID** → هذا هو `VERCEL_PROJECT_ID`
   - **Team ID** أو **Organization ID** → هذا هو `VERCEL_ORG_ID`
6. **انسخ القيم**

### الخطوة 2: إضافة Secrets في GitHub

1. اذهب إلى مستودع GitHub: `https://github.com/halawy50/the-dunes`
2. اضغط **Settings** (في القائمة العلوية)
3. اضغط **Secrets and variables** → **Actions**
4. اضغط **"New repository secret"**
5. أضف الـ 3 Secrets التالية:

   **Secret 1:**
   - Name: `VERCEL_TOKEN`
   - Value: (الصق الـ Token من الخطوة 1.1)
   - اضغط **"Add secret"**

   **Secret 2:**
   - Name: `VERCEL_ORG_ID`
   - Value: (الصق Organization/Team ID من الخطوة 1.2)
   - اضغط **"Add secret"**

   **Secret 3:**
   - Name: `VERCEL_PROJECT_ID`
   - Value: (الصق Project ID من الخطوة 1.2)
   - اضغط **"Add secret"**

### الخطوة 3: رفع التحديثات على Git

بعد إضافة Secrets، ارفع التحديثات:

```powershell
# 1. انتقل إلى مجلد المشروع
cd D:\the_dunes

# 2. أضف جميع التغييرات
git add .

# 3. احفظ التغييرات
git commit -m "Update: تحديثات جديدة"

# 4. ارفع على GitHub
git push origin main
```

### الخطوة 4: مراقبة النشر

بعد الرفع:

1. **تحقق من GitHub Actions:**
   - اذهب إلى: `https://github.com/halawy50/the-dunes/actions`
   - ستجد workflow جديد يبدأ تلقائياً
   - اضغط عليه لرؤية التقدم

2. **تحقق من Vercel:**
   - اذهب إلى: [vercel.com/dashboard](https://vercel.com/dashboard)
   - افتح مشروع **"the-dunes"**
   - ستجد deployment جديد

3. **تحقق من الموقع:**
   - افتح: **https://the-dunes.vercel.app**
   - اضغط `Ctrl + F5` لإزالة التخزين المؤقت
   - التحديثات ستظهر!

## 🎉 النتيجة

بعد الإعداد، عند كل push على `main`:
- ✅ GitHub Actions يبدأ البناء تلقائياً
- ✅ يبني Flutter web app
- ✅ ينشر على Vercel تلقائياً
- ✅ الموقع يتم تحديثه في دقائق!

## 🔄 للمستقبل

بعد الإعداد الأولي، فقط:

```powershell
git add .
git commit -m "وصف التحديثات"
git push origin main
```

**هذا كل شيء!** النشر التلقائي سيقوم بالباقي. 🚀

## ⚠️ ملاحظات مهمة

1. **تأكد من ربط المشروع بـ "the-dunes":**
   - في Vercel Dashboard، تأكد أن المشروع اسمه "the-dunes"
   - إذا كان اسمه "web"، قم بتغييره أو أنشئ مشروع جديد باسم "the-dunes"

2. **Branch Names:**
   - النشر التلقائي يعمل على: `main`, `master`, `development`
   - تأكد أن branch الحالي هو أحد هذه الأسماء

3. **إذا فشل النشر:**
   - تحقق من GitHub Actions logs
   - تأكد أن جميع Secrets موجودة وصحيحة
   - تحقق من Vercel Dashboard للأخطاء

## 📞 مساعدة

إذا واجهت مشاكل:
1. تحقق من ملف `GITHUB_VERCEL_SETUP.md` للتفاصيل الكاملة
2. تحقق من GitHub Actions logs للأخطاء
3. تأكد من أن جميع Secrets موجودة في GitHub

